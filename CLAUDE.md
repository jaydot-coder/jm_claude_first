# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`jm_claude_first`) is currently empty scaffolding — it contains only this file and `README.md`. There is no source code, build system, dependency manifest, test suite, or configuration yet.

Because there is nothing to build, lint, test, or run, this file intentionally does not list any such commands. Once real code is added, update this file with:

- The actual build/lint/test/run commands (and how to run a single test)
- The high-level architecture and how the major pieces fit together
- Any project-specific conventions

Do not invent commands or architecture ahead of the code actually existing here.
